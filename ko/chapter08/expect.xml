<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
  "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../general.ent">
  %general-entities;
]>

<sect1 id="ch-system-expect" role="wrap">
  <?dbhtml filename="expect.html"?>

  <sect1info condition="script">
<productname>expect</productname>
<productnumber>&expect-version;</productnumber> <address>&expect-url;</address></sect1info>

  <title>Expect-&expect-version;</title>

  <indexterm zone="ch-system-expect">
    <primary sortas="a-Expect">Expect</primary>
  </indexterm>

  <sect2 role="package">
    <title/>

    <para><application>Expect</application> 패키지에는 <command>telnet</command>,
<command>ftp</command>, <command>passwd</command>, <command>fsck</command>,
<command>rlogin</command>, <command>tip</command> 같은 대화형 애플리케이션을 스크립트 대화
방식으로 자동화하는 도구가 들어 있습니다. 또한 <application>Expect</application> 는 이러한 애플리케이션을
테스트하는 데 유용할 뿐만 아니라, 다른 도구로는 수행하기 매우 어려운 온갖 종류의 작업을 쉽게 만들어
줍니다. <application>DejaGnu</application> 프레임워크는
<application>Expect</application>로 만들었습니다.</para>

    <segmentedlist>
      <segtitle>&buildtime;</segtitle>
      <segtitle>&diskspace;</segtitle>

      <seglistitem>
        <seg>&expect-tmp-sbu;</seg>
        <seg>&expect-tmp-du;</seg>
      </seglistitem>
    </segmentedlist>

  </sect2>

  <sect2 role="installation">
    <title>Expect 설치</title>

    <para>Expect needs PTYs to work.  Verify that the PTYs are working properly inside
the chroot environment by performing a simple test:</para>

<screen><userinput remap="test">python3 -c 'from pty import spawn; spawn(["echo", "ok"])'</userinput></screen>

    <para>This command should output <computeroutput>ok</computeroutput>.  If,
instead, the output includes <computeroutput>OSError: out of pty
devices</computeroutput>, then the environment is not set up for proper PTY
operation.  You need to exit from the chroot environment, read <xref
linkend='ch-tools-kernfs'/> again, and ensure the <systemitem
class="filesystem">devpts</systemitem> file system (and other virtual kernel
file systems) mounted correctly.  Then reenter the chroot environment
following <xref linkend='ch-tools-chroot'/>.  This issue needs to be
resolved before continuing, or the test suites requiring Expect (for example
the test suites of Bash, Binutils, GCC, GDBM, and of course Expect itself)
will fail catastrophically, and other subtle breakages may also happen.</para>

    <para>Now, make some changes to allow the package with gcc-15.1 or later:</para>

<screen><userinput remap="pre">patch -Np1 -i ../&expect-gcc15-patch;</userinput></screen>

    <para>Expect 컴파일 준비하기:</para>

<screen><userinput remap="configure">./configure --prefix=/usr           \
            --with-tcl=/usr/lib     \
            --enable-shared         \
            --disable-rpath         \
            --mandir=/usr/share/man \
            --with-tclinclude=/usr/include</userinput></screen>

    <variablelist>
      <title>configure 옵션 뜻:</title>

      <varlistentry>
        <term><parameter>--with-tcl=/usr/lib</parameter></term>
        <listitem>
          <para>This parameter is needed to tell <command>configure</command> where the
<command>tclConfig.sh</command> script is located.</para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term><parameter>--with-tclinclude=/usr/include</parameter></term>
        <listitem>
          <para>This explicitly tells Expect where to find Tcl's internal headers.</para>
        </listitem>
      </varlistentry>

    </variablelist>

    <para>패키지 빌드:</para>

<screen><userinput remap="make">make</userinput></screen>

    <para>결과를 테스트하려면 실행:</para>

<screen><userinput remap="test">make test</userinput></screen>

    <para>패키지 설치:</para>

<screen><userinput remap="install">make install
ln -svf expect&expect-version;/libexpect&expect-version;.so /usr/lib</userinput></screen>

  </sect2>

  <sect2 id="contents-expect" role="content">
    <title>Expect 구성 요소</title>

    <segmentedlist>
      <segtitle>설치된 프로그램</segtitle>
      <segtitle>설치된 라이브러리</segtitle>

      <seglistitem>
        <seg>expect</seg>
        <seg>libexpect&expect-version;.so</seg>
      </seglistitem>
    </segmentedlist>

    <variablelist>
      <bridgehead renderas="sect3">짧은 설명</bridgehead>
      <?dbfo list-presentation="list"?> <?dbhtml list-presentation="table"?>

      <varlistentry id="expect">
        <term><command>expect</command></term>
        <listitem>
          <para>Communicates with other interactive programs according to a script</para>
          <indexterm zone="ch-system-expect expect">
            <primary sortas="b-expect">expect</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="libexpect">
        <term><filename class="libraryfile">libexpect-&expect-version;.so</filename></term>
        <listitem>
          <para>Contains functions that allow Expect to be used as a Tcl extension or to be
used directly from C or C++ (without Tcl)</para>
          <indexterm zone="ch-system-expect libexpect">
            <primary sortas="c-libexpect-&expect-version;">libexpect-&expect-version;</primary>
          </indexterm>
        </listitem>
      </varlistentry>

    </variablelist>

  </sect2>

</sect1>
