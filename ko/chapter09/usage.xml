<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
  "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../general.ent">
  <!ENTITY site               SYSTEM "../appendices/rc.site.script">
  %general-entities;
]>

<sect1 id="ch-config-usage" revision="sysv">
  <?dbhtml filename="usage.html"?>

  <title>System V 부트스크립트 사용법 및 설정</title>

  <indexterm zone="ch-config-usage">
    <primary sortas="a-Bootscripts">Bootscripts</primary>
    <secondary>usage</secondary>
  </indexterm>

  <sect2>
    <title>System V 부트스크립트는 어떻게 동작하는가?</title>

    <para>이 버전 LFS는 일련의 <emphasis>run level</emphasis>에 기반한 SysVinit이라는 특수한 부팅 기능을
씁니다. 부팅 절차는 시스템마다 매우 다를 수 있습니다. 특정 리눅스 배포판에서 어떤 방식이 동작했다고 해서 LFS에서도 똑같이 된다는
보장은 없습니다. LFS는 자신만의 고유한 작동 방식을 가지고 있지만, 일반적으로 수용되는 표준을 준수합니다.</para>
  
    <para><command>systemd</command> 라고 불리는 대안적인 부팅 절차가 있습니다. 여기서는 해당 부팅 프로세스에 대해 더 이상
논의하지 않겠습니다. 자세한 설명은 <ulink
url="https://www.linux.com/training-tutorials/understanding-and-using-systemd/"/>
참조하세요.</para>

    <para>SysVinit(지금부터는 <quote>init</quote>이라고 부릅니다)은 런레벨 스키마를 사용합니다. 런레벨은 0부터 6까지 총
7개가 있습니다. (실제로는 더 많은 런레벨이 있지만, 나머지는 특수한 경우를 위한 것이며 일반적으로는 사용되지 않습니다. 자세한 내용은
<ulink role='man' url='&man;init.8'>init(8)</ulink> 매뉴얼 페이지를 참조하십시오.) 7개의 각
런레벨은 컴퓨터가 시작되거나 종료될 때 수행해야 하는 동작들에 대응합니다. 기본 런레벨은 3입니다. 다음은 LFS에 구현된 각 런레벨에
대한 설명:</para>

<literallayout>0: halt the computer
1: single-user mode
2: reserved for customization, otherwise the same as 3
3: multi-user mode with networking
4: reserved for customization, otherwise the same as 3
5: same as 4, it is usually used for GUI login (like GNOME's <command>gdm</command> or LXDE's <command>lxdm</command>)
6: reboot the computer</literallayout>

    <note>
       <para>
         전통적으로 위의 런 레벨 2는 <quote>네트워킹 없는 다중 사용자 모드</quote>로 정의되었습니다. 하지만 이는 여러 사용자가
시리얼 포트를 통해 시스템에 접속할 수 있었던 아주 오래전의 경우에만 해당합니다. 오늘날의 환경에서 이는 의미가 없으므로, 현재는 이
레벨을 <quote>예약됨(reserved)</quote> 상태라고 부릅니다
       </para>
    </note>

  </sect2>

  <sect2 id="conf-sysvinit" role="configuration">
  <title>SysVinit 설정</title>

  <indexterm zone="conf-sysvinit">
    <primary sortas="a-SysVinit">SysVinit</primary>
    <secondary>configuring</secondary>
  </indexterm>

  <indexterm zone="conf-sysvinit">
    <primary sortas="e-/etc/inittab">/etc/inittab</primary>
  </indexterm>

  <para>커널 초기화 중에 (커맨드 라인에서 재정의되지 않는 한) 가장 먼저 실행되는 프로그램은 <command>init</command>
입니다. 이 프로그램은 초기화 파일인 <filename>/etc/inittab</filename>을 읽습니다. 다음을 사용하여 이 파일을
생성:</para>

<screen><userinput>cat &gt; /etc/inittab &lt;&lt; "EOF"
<literal># Begin /etc/inittab

id:3:initdefault:

si::sysinit:/etc/rc.d/init.d/rc S

l0:0:wait:/etc/rc.d/init.d/rc 0
l1:S1:wait:/etc/rc.d/init.d/rc 1
l2:2:wait:/etc/rc.d/init.d/rc 2
l3:3:wait:/etc/rc.d/init.d/rc 3
l4:4:wait:/etc/rc.d/init.d/rc 4
l5:5:wait:/etc/rc.d/init.d/rc 5
l6:6:wait:/etc/rc.d/init.d/rc 6

ca:12345:ctrlaltdel:/sbin/shutdown -t1 -a -r now

su:S06:once:/sbin/sulogin
s1:1:respawn:/sbin/sulogin

1:2345:respawn:/sbin/agetty --noclear tty1 9600
2:2345:respawn:/sbin/agetty tty2 9600
3:2345:respawn:/sbin/agetty tty3 9600
4:2345:respawn:/sbin/agetty tty4 9600
5:2345:respawn:/sbin/agetty tty5 9600
6:2345:respawn:/sbin/agetty tty6 9600

# End /etc/inittab</literal>
EOF</userinput></screen>

  <para>이 초기화 파일에 대한 설명은 <emphasis>inittab</emphasis> 맨 페이지(man page)에 나와
있습니다. LFS에서 핵심 명령은 <command>rc</command> 입니다. 위의 초기화 파일은 <filename
class="directory">/etc/rc.d/rcS.d</filename> 디렉터리에 있는 S로 시작하는 모든 스크립트를 실행한
다음, initdefault 값으로 지정된 <filename
class="directory">/etc/rc.d/rc?.d</filename> 디렉터리의 S로 시작하는 모든 스크립트를 실행하도록
<command>rc</command>에 지시합니다.</para>

  <para>편의를 위해, <command>rc</command> 스크립트는 <filename
class="directory">/lib/lsb/init-functions</filename> 디렉터리에 있는 함수 라이브러리를
읽습니다. 또한 이 라이브러리는 선택 사항인 설정 파일 <filename>/etc/sysconfig/rc.site</filename>을
읽습니다. 이후 섹션에서 설명하는 모든 시스템 설정 매개변수를 이 파일에 넣을 수 있으며, 이를 통해 모든 시스템 매개변수를 이 하나의
파일로 통합하여 구축할 수 있습니다.</para>

  <para>디버깅 편의를 위해, 함수 스크립트는 모든 출력을 <filename>/run/var/bootlog</filename> 에
기록합니다. <filename class="directory">/run</filename> 디렉터리는 tmpfs 이므로 이 파일은 부팅
간에 유지되지 않지만, 부팅 프로세스가 끝날 때 더 영구적인 파일인 <filename>/var/log/boot.log</filename>
뒤에 추가됩니다.</para>

  <sect3 id="init-levels" >
  <title>Run Level 바꾸기</title>

  <para>Changing run levels is done with <command>init
<replaceable>&lt;runlevel&gt;</replaceable></command>, where
<replaceable>&lt;runlevel&gt;</replaceable> is the target run level. For
example, to reboot the computer, a user could issue the <command>init
6</command> command, which is an alias for the <command>reboot</command>
command. Likewise, <command>init 0</command> is an alias for the
<command>halt</command> command.</para>

  <para>There are a number of directories under <filename
class="directory">/etc/rc.d</filename> that look like <filename
class="directory">rc?.d</filename> (where ? is the number of the run level)
and <filename class="directory">rcS.d</filename>, all containing a number of
symbolic links. Some links begin with a <emphasis>K</emphasis>; the others
begin with an <emphasis>S</emphasis>, and all of them have two numbers
following the initial letter. The K means to stop (kill) a service and the S
means to start a service. The numbers determine the order in which the
scripts are run, from 00 to 99&mdash;the smaller the number, the sooner the
script runs. When <command>init</command> switches to another run level, the
appropriate services are either started or stopped, depending on the run
level chosen.</para>

  <para>The real scripts are in <filename
class="directory">/etc/rc.d/init.d</filename>. They do the actual work, and
the symlinks all point to them. K links and S links point to the same script
in <filename class="directory">/etc/rc.d/init.d</filename>.  This is because
the scripts can be called with different parameters like
<parameter>start</parameter>, <parameter>stop</parameter>,
<parameter>restart</parameter>, <parameter>reload</parameter>, and
<parameter>status</parameter>. When a K link is encountered, the appropriate
script is run with the <parameter>stop</parameter> argument. When an S link
is encountered, the appropriate script is run with the
<parameter>start</parameter> argument.</para>

  <!-- Changed for March 24th, 2022 version of bootscripts
  <para>
There is one exception to this explanation. Links that start
  with an <emphasis>S</emphasis> in the <filename
  class="directory">rc0.d</filename> and <filename
  class="directory">rc6.d</filename> directories will not cause anything
  to be started. They will be called with the parameter
  <parameter>stop</parameter> to stop something. The logic behind this
  is that when a user is going to reboot or halt the system, nothing
  needs to be started, but the order of shutdown needs to be controlled.
  For these run levels, all <emphasis>K</emphasis> prefixed scripts will be
  run before any <emphasis>S</emphasis> prefixed scripts are run with the
  <parameter>stop</parameter> parameter.
  </para>
-->
<para>These are descriptions of what the arguments make the scripts do:</para>

  <variablelist>

    <varlistentry>
      <term><parameter>start</parameter></term>
      <listitem>
        <para>서비스 시작.</para>
      </listitem>
    </varlistentry>

    <varlistentry>
      <term><parameter>stop</parameter></term>
      <listitem>
        <para>서비스 멈춤.</para>
      </listitem>
    </varlistentry>

    <varlistentry>
      <term><parameter>restart</parameter></term>
      <listitem>
        <para>The service is stopped and then started again.</para>
      </listitem>
    </varlistentry>

    <varlistentry>
      <term><parameter>reload</parameter></term>
      <listitem>
        <para>The configuration of the service is updated.  This is used after the
configuration file of a service was modified, when the service does not need
to be restarted.</para>
      </listitem>
    </varlistentry>

    <varlistentry>
      <term><parameter>status</parameter></term>
      <listitem>
        <para>Tells if the service is running and with which PIDs.</para>
      </listitem>
    </varlistentry>

  </variablelist>

  <para>Feel free to modify the way the boot process works (after all, it is your
own LFS system). The files given here are an example of how it can be done.</para>

  </sect3>
  </sect2>

  <sect2>
    <title>Udev Bootscripts</title>

      <para>The <filename>/etc/rc.d/init.d/udev</filename> initscript starts
<command>udevd</command>, triggers any "coldplug" devices that have already
been created by the kernel, and waits for any rules to complete.  The script
also unsets the uevent handler from the default of <filename>/sbin/hotplug
</filename>.  This is done because the kernel no longer needs to call an
external binary.  Instead, <command>udevd</command> will listen on a netlink
socket for uevents that the kernel raises.</para>

      <para>The <command>/etc/rc.d/init.d/udev_retry</command> script takes care of
re-triggering events for subsystems whose rules may rely on file systems
that are not mounted until the <command>mountfs</command> script is run (in
particular, <filename class="directory">/usr</filename> and <filename
class="directory">/var</filename> may cause this).  This script runs after
the <command>mountfs</command> script, so those rules (if re-triggered)
should succeed the second time around.  It is configured by the
<filename>/etc/sysconfig/udev_retry</filename> file; any words in this file
other than comments are considered subsystem names to trigger at retry
time.  To find the subsystem of a device, use <command>udevadm info
--attribute-walk &lt;device&gt;</command> where &lt;device&gt; is an
absolute path in /dev or /sys, such as /dev/sr0, or /sys/class/rtc.</para>

      <para>For information on kernel module loading and udev, see <xref
linkend="module-loading" role='.'/></para>
  </sect2>

  <sect2 id="ch-config-clock">
    <title>시스템 clock 설정</title>

    <indexterm zone="ch-config-clock">
      <primary sortas="d-setclock">setclock</primary>
    <secondary>configuring</secondary></indexterm>

    <para><command>setclock</command> 스크립트는 BIOS 또는 CMOS 시계로도 알려진 하드웨어 시계로부터 시간을
읽어옵니다. 하드웨어 시계가 UTC로 설정되어 있다면, 이 스크립트는 <filename>/etc/localtime</filename>
파일(<command>hwclock</command> 프로그램에 어떤 시간대를 사용할지 알려주는 파일)을 사용하여 하드웨어 시계의 시간을
현지 시간으로 변환합니다. 하드웨어 시계가 UTC로 설정되어 있는지 아닌지 자동으로 감지할 방법은 없으므로, 이를 수동으로 설정해야
합니다.</para>

    <para>The <command>setclock</command> program is run via
<application>udev</application> when the kernel detects the hardware
capability upon boot.  It can also be run manually with the stop parameter
to store the system time to the CMOS clock.</para>

    <para>하드웨어 시계가 UTC로 설정되어 있는지 생각 안 나면, <userinput>hwclock --localtime
--show</userinput> 명령어를 실행하여 확인하십시오. 이 명령어는 하드웨어 시계에 기록된 현재 시간을 표시합니다. 이 시간이
당신의 시계와 일치한다면 하드웨어 시계는 현지 시간으로 설정된 것입니다. 만약 <command>hwclock</command>의 출력
결과가 현지 시간과 다르다면, UTC 시간으로 설정되어 있을 가능성이 높습니다. <command>hwclock</command>이 표시한
시간에 당신이 있는 시간대의 시차만큼 시간을 더하거나 빼서 이를 확인할 수 있습니다. 예를 들어, 현재 GMT -0700으로 알려진
MST 시간대에 있다면, 우리가 확인한 현지 시간에 7시간을 더하시오.</para>

    <para>Change the value of the <envar>UTC</envar> variable below to a value of
<parameter>0</parameter> (zero) if the hardware clock is
<emphasis>NOT</emphasis> set to UTC time.</para>

    <para>Create a new file <filename>/etc/sysconfig/clock</filename> by running the
following:</para>

<screen><userinput>cat &gt; /etc/sysconfig/clock &lt;&lt; "EOF"
<literal># Begin /etc/sysconfig/clock

UTC=1

# Set this to any options you might need to give to hwclock,
# such as machine hardware clock type for Alphas.
CLOCKPARAMS=

# End /etc/sysconfig/clock</literal>
EOF</userinput></screen>

    <para>A good hint explaining how to deal with time on LFS is available at <ulink
url="&hints-root;time.txt"/>. It explains issues such as time zones, UTC,
and the <envar>TZ</envar> environment variable.</para>

    <note><para>The CLOCKPARAMS and UTC parameters may also be set in the
<filename>/etc/sysconfig/rc.site</filename> file.</para></note>

  </sect2>

  <sect2 id="ch-config-console">
  <?dbhtml filename="console.html"?>

  <title>Configuring the Linux Console</title>

  <indexterm zone="ch-config-console">
    <primary sortas="d-console">console</primary>
    <secondary>configuring</secondary>
  </indexterm>

  <para>이 섹션에서는 키보드 맵, 콘솔 폰트 및 콘솔 커널 로그 레벨을 설정하는 <command>console</command> 부트스크립트를
구성하는 방법에 대해 논의합니다. 비 ASCII 문자(예: 저작권 기호, 영국 파운드 기호, 유로 기호 등)를 사용하지 않고 미국식
키보드를 사용하는 경우, 이 섹션의 상당 부분을 건너뛸 수 있습니다. 설정 파일(또는
<filename>rc.site</filename>의 동일한 설정)이 없으면, <command>console</command> 부트
스크립트는 아무 작업도 안 합니다.</para>

  <para><command>console</command> 스크립트는 설정 정보를 얻기 위해
<filename>/etc/sysconfig/console</filename> 파일을 읽습니다. 어떤 키맵(keymap)과 화면
폰트(screen font)를 사용할지 결정하십시오. 다양한 언어별 HOWTO 문서도 도움이 될 수 있습니다. <ulink
url="https://tldp.org/HOWTO/HOWTO-INDEX/other-lang.html"/> 참조하세요. 여전히 확신이 서지
않는다면, 유효한 키맵과 화면 폰트를 찾기 위해 <filename
class="directory">/usr/share/keymaps</filename> 및 <filename
class="directory">/usr/share/consolefonts</filename> 디렉터리를 확인해 보십시오. 이러한
프로그램에 사용할 올바른 인수를 결정하려면 <ulink role='man'
url='&man;loadkeys.1'>loadkeys(1)</ulink> 및 <ulink role='man'
url='&man;setfont.8'>setfont(8)</ulink> 맨 페이지를 읽어보십시오.</para>

  <para><filename>/etc/sysconfig/console</filename> 파일은
<envar>VARIABLE=value</envar> 형식의 행을 포함해야 합니다. 다음 변수 인식:</para>
  <variablelist>

    <varlistentry>
      <term>LOGLEVEL</term>
      <listitem>
        <para>This variable specifies the log level for kernel messages sent to the
console as set by <command>dmesg -n</command>. Valid levels are from
<literal>1</literal> (no messages) to <literal>8</literal>. The default
level is <literal>7</literal>, which is quite verbose.</para>
      </listitem>
    </varlistentry>

    <varlistentry>
      <term>KEYMAP</term>
      <listitem>
        <para>This variable specifies the arguments for the <command>loadkeys</command>
program, typically, the name of the keymap to load, e.g.,
<literal>it</literal>. If this variable is not set, the bootscript will not
run the <command>loadkeys</command> program, and the default kernel keymap
will be used.  Note that a few keymaps have multiple versions with the same
name (cz and its variants in qwerty/ and qwertz/, es in olpc/ and qwerty/,
and trf in fgGIod/ and qwerty/). In these cases the parent directory should
also be specified (e.g. qwerty/es) to ensure the proper keymap is loaded.
        </para>
      </listitem>
    </varlistentry>

    <varlistentry>
      <term>KEYMAP_CORRECTIONS</term>
      <listitem>
        <para>This (rarely used) variable specifies the arguments for the second call to
the <command>loadkeys</command> program. This is useful if the stock keymap
is not completely satisfactory and a small adjustment has to be made. E.g.,
to include the Euro sign into a keymap that normally doesn't have it, set
this variable to <literal>euro2</literal>.</para>
      </listitem>
    </varlistentry>

    <varlistentry>
      <term>FONT</term>
      <listitem>
        <para><!-- because of the copyright sign -->
이 변수는 <command>setfont</command> 프로그램 인수를 상술합니다. 전형적으로 여기에는 글꼴 이름,
<literal>-m</literal>, 로드할 애플리케이션 문자 맵의 이름이 들어갑니다. 예를 들어,
<quote>lat1-16</quote> 글꼴과 <quote>8859-1</quote> 애플리케이션 문자 맵(미국에 적합)을 함께
로드하려면, 이 변수를 <literal>lat1-16 -m 8859-1</literal>로 설정하십시오. UTF-8 모드에서 커널은
애플리케이션 문자 맵을 사용하여 8비트 키 코드를 UTF-8로 변환합니다. 따라서 "-m" 매개변수 인수는 키맵에서 조합된 키 코드의
인코딩으로 설정해야 할 겁니다.</para>

      </listitem>
    </varlistentry>

    <varlistentry>
      <term>UNICODE</term>
      <listitem>
        <para>Set this variable to <literal>1</literal>, <literal>yes</literal>, or
<literal>true</literal> in order to put the console into UTF-8 mode. This is
useful in UTF-8 based locales and harmful otherwise.</para>
      </listitem>
    </varlistentry>

    <varlistentry>
      <term>LEGACY_CHARSET</term>
      <listitem>
        <para>For many keyboard layouts, there is no stock Unicode keymap in the Kbd
package. The <command>console</command> bootscript will convert an available
keymap to UTF-8 on the fly if this variable is set to the encoding of the
available non-UTF-8 keymap.</para>
      </listitem>
    </varlistentry>

  </variablelist>

  <para>몇 예제:</para>

  <itemizedlist>
    <listitem>
      <para>
        We'll use <literal>C.UTF-8</literal> as the locale for interactive sessions
in the Linux console in <xref role=',' linkend='ch-config-locale'/> so we
should set <literal>UNICODE</literal> to <literal>1</literal>.  And the
console fonts shipped by the <application>Kbd</application> package
containing the glyphs for all characters from the program messages in the
<literal>C.UTF-8</literal> locale are
<filename>LatArCyrHeb*.psfu.gz</filename>,
<filename>LatGrkCyr*.psfu.gz</filename>,
<filename>Lat2-Terminus16.psfu.gz</filename>, and
<filename>pancyrillic.f16.psfu.gz</filename> in <filename
class='directory'>/usr/share/consolefonts</filename> (the other shipped
console fonts lack glyphs of some characters like the Unicode left/right
quotation marks and the Unicode English dash).  So set one of them, for
example <filename>Lat2-Terminus16.psfu.gz</filename> as the default console
font:
      </para>

      
<screen role='nodump'><!-- We are using 'nodump' for the generic setting, but not for an
           example (see below).  This is deliberate to keep the
           compatibility with jhalfs.  -->
<userinput>cat &gt; /etc/sysconfig/console &lt;&lt; "EOF"
<literal># Begin /etc/sysconfig/console

UNICODE="1"
FONT="Lat2-Terminus16"

# End /etc/sysconfig/console</literal>
EOF</userinput></screen>

    </listitem>

    <listitem>
      <para>For a non-Unicode setup, only the KEYMAP and FONT variables are generally
needed. E.g., for a Polish setup, one would use:</para>
<screen role="nodump"><userinput>cat &gt; /etc/sysconfig/console &lt;&lt; "EOF"
<literal># Begin /etc/sysconfig/console

KEYMAP="pl2"
FONT="lat2a-16 -m 8859-2"

# End /etc/sysconfig/console</literal>
EOF</userinput></screen>
    </listitem>

    <listitem>
      <para>As mentioned above, it is sometimes necessary to adjust a stock keymap
slightly. The following example adds the Euro symbol to the German keymap:</para>

<screen role="nodump"><userinput>cat &gt; /etc/sysconfig/console &lt;&lt; "EOF"
<literal># Begin /etc/sysconfig/console

KEYMAP="de-latin1"
KEYMAP_CORRECTIONS="euro2"
FONT="lat0-16 -m 8859-15"
UNICODE="1"

# End /etc/sysconfig/console</literal>
EOF</userinput></screen>
    </listitem>

    <listitem>
      <para>The following is a Unicode-enabled example for Bulgarian, where a stock
UTF-8 keymap exists:</para>

<screen><!-- This is what is used by jhalfs for creating the console file: whenever
     you change the following, please inform the jhalfs maintainer(s). -->
<userinput>cat &gt; /etc/sysconfig/console &lt;&lt; "EOF"
<literal># Begin /etc/sysconfig/console

UNICODE="1"
KEYMAP="bg_bds-utf8"
FONT="LatArCyrHeb-16"

# End /etc/sysconfig/console</literal>
EOF</userinput></screen>
    </listitem>

    <listitem>
      <para>Due to the use of a 512-glyph LatArCyrHeb-16 font in the previous example,
bright colors are no longer available on the Linux console unless a
framebuffer is used. If one wants to have bright colors without a
framebuffer and can live without characters not belonging to his language,
it is still possible to use a language-specific 256-glyph font, as
illustrated below:</para>

<screen role="nodump"><userinput>cat &gt; /etc/sysconfig/console &lt;&lt; "EOF"
<literal># Begin /etc/sysconfig/console

UNICODE="1"
KEYMAP="bg_bds-utf8"
FONT="cyr-sun16"

# End /etc/sysconfig/console</literal>
EOF</userinput></screen>
    </listitem>

    <listitem>
      <para>The following example illustrates keymap autoconversion from ISO-8859-15 to
UTF-8 and enabling dead keys in Unicode mode:</para>

<screen role="nodump"><userinput>cat &gt; /etc/sysconfig/console &lt;&lt; "EOF"
<literal># Begin /etc/sysconfig/console

UNICODE="1"
KEYMAP="de-latin1"
KEYMAP_CORRECTIONS="euro2"
LEGACY_CHARSET="iso-8859-15"
FONT="LatArCyrHeb-16 -m 8859-15"

# End /etc/sysconfig/console</literal>
EOF</userinput></screen>
    </listitem>

    <listitem>
      <para>Some keymaps have dead keys (i.e., keys that don't produce a character by
themselves, but put an accent on the character produced by the next key) or
define composition rules (such as: <quote>press Ctrl+. A E to get
&AElig;</quote> in the default keymap).  Linux-&linux-version; interprets
dead keys and composition rules in the keymap correctly only when the source
characters to be composed together are not multibyte. This deficiency
doesn't affect keymaps for European languages, because there accents are
added to unaccented ASCII characters, or two ASCII characters are composed
together. However, in UTF-8 mode it is a problem; e.g., for the Greek
language, where one sometimes needs to put an accent on the letter α.  The
solution is either to avoid the use of UTF-8, or to install the X window
system, which doesn't have this limitation, in its input handling.</para>
    </listitem>

    <listitem>
      <para>For Chinese, Japanese, Korean, and some other languages, the Linux console
cannot be configured to display the needed characters. Users who need such
languages should install the X Window System, fonts that cover the necessary
character ranges, and the proper input method (e.g., SCIM supports a wide
variety of languages).</para>
    </listitem>

  </itemizedlist>

  
  <!-- Added because folks keep posting their console file with X questions
  to blfs-support list -->
<note>
    <para><filename>/etc/sysconfig/console</filename> 파일은 오직 리눅스 텍스트 콘솔의 로캘 설정만을
제어합니다. 이 파일은 X 윈도우 시스템에서의 적절한 키보드 레이아웃 및 터미널 글꼴 설정, ssh 세션, 또는 시리얼 콘솔과는 아무런
관련이 없습니다. 이러한 상황에서는 위 목록의 마지막 두 항목에서 언급된 제한 사항들이 적용 안 됩니다.</para>
  </note>

  </sect2>

  <sect2 id="ch-config-createfiles">
    <title>부트 시 파일 생성</title>

    <indexterm zone="ch-config-createfiles">
      <primary sortas="d-createfiles">File creation at boot</primary>
      <secondary>configuring</secondary>
    </indexterm>

    <para>At times, it is desirable to create files at boot time.  For instance, the
<filename class="directory">/tmp/.ICE-unix</filename> directory is often
needed.  This can be done by creating an entry in the
<filename>/etc/sysconfig/createfiles</filename> configuration script.  The
format of this file is embedded in the comments of the default configuration
file.</para>
  </sect2>

  <sect2 id="ch-config-sysklogd">
    <title>Configuring the Sysklogd Script</title>

    <indexterm zone="ch-config-sysklogd">
      <primary sortas="d-sysklogd">sysklogd</primary>
      <secondary>configuring</secondary>
    </indexterm>

    <para>The <filename>sysklogd</filename> script invokes the
<command>syslogd</command> program as a part of System V initialization.
The <parameter>-m 0</parameter> option turns off the periodic timestamp mark
that <command>syslogd</command> writes to the log files every 20 minutes by
default.  If you want to turn on this periodic timestamp mark, edit
<filename>/etc/sysconfig/rc.site</filename> and define the variable
SYSKLOGD_PARMS to the desired value.  For instance, to remove all
parameters, set the variable to a null value:</para>

<screen role="nodump">SYSKLOGD_PARMS=</screen>

    <para>See <userinput>man syslogd</userinput> for more options.</para>

  </sect2>

  <sect2 id="ch-config-site">
    <title>The rc.site File</title>

    <indexterm zone="ch-config-site">
      <primary sortas="a-rc.site">rc.site</primary>
    </indexterm>

    <para>The optional <filename>/etc/sysconfig/rc.site</filename> file contains
settings that are automatically set for each SystemV boot script.  It can
alternatively set the values specified in the <filename>hostname</filename>,
<filename>console</filename>, and <filename>clock</filename> files in the
<filename class='directory'>/etc/sysconfig/</filename> directory.  If the
associated variables are present in both these separate files and
<filename>rc.site</filename>, the values in the script-specific files take
effect. </para>

    <para><filename>rc.site</filename> also contains parameters that can customize
other aspects of the boot process.  Setting the IPROMPT variable will enable
selective running of bootscripts.  Other options are described in the file
comments.  The default version of the file is as follows:</para>

    
    <screen role="auto"><!-- Use role to fix a pdf generation problem -->
&site;</screen>

    <sect3>
      <title>부트 및 셧다운 스크립트 커스터마이징</title>

      <para>The LFS boot scripts boot and shut down a system in a fairly efficient
manner, but there are a few tweaks you can make in the rc.site file to
improve speed even more, and to adjust messages according to your
preferences. To do this, adjust the settings in the
<filename>/etc/sysconfig/rc.site</filename> file above.</para>

      <itemizedlist>

      <listitem><para>During the boot script <filename>udev</filename>, there is a call to
<command>udev settle</command> that requires some time to complete. This
time may or may not be required depending on the devices in the system.  If
you only have simple partitions and a single ethernet card, the boot process
will probably not need to wait for this command.  To skip it, set the
variable OMIT_UDEV_SETTLE=y.</para></listitem>

      <listitem><para>The boot script <filename>udev_retry</filename> also runs <command>udev
settle</command> by default.  This command is only needed if the <filename
class='directory'>/var</filename> directory is separately mounted, because
the clock needs the <filename>/var/lib/hwclock/adjtime</filename> file.
Other customizations may also need to wait for udev to complete, but in many
installations it is not necessary.  Skip the command by setting the variable
OMIT_UDEV_RETRY_SETTLE=y.
      </para></listitem>

      <listitem><para>By default, the file system checks are silent.  This can appear to be a
delay during the bootup process.  To turn on the <command>fsck</command>
output, set the variable VERBOSE_FSCK=y.
      </para></listitem>

      <listitem><para>When rebooting, you may want to skip the filesystem check,
<command>fsck</command>, completely.  To do this, either create the file
<filename>/fastboot</filename> or reboot the system with the command
<command>/sbin/shutdown -f -r now</command>.  On the other hand, you can
force all file systems to be checked by creating
<filename>/forcefsck</filename> or running <command>shutdown</command> with
the <parameter>-F</parameter> parameter instead of
<parameter>-f</parameter>.
      </para>

      <para>Setting the variable FASTBOOT=y will disable <command>fsck</command> during
the boot process until it is removed.  This is not recommended on a
permanent basis.</para></listitem>

      <listitem><para>Normally, all files in the <filename class='directory'>/tmp</filename>
directory are deleted at boot time.  Depending on the number of files or
directories present, this can cause a noticeable delay in the boot process.
To skip removing these files set the variable SKIPTMPCLEAN=y.</para></listitem>

      <listitem><para>During shutdown, the <command>init</command> program sends a TERM signal to
each program it has started (e.g. agetty), waits for a set time (default 3
seconds), then sends each process a KILL signal and waits again.  This
process is repeated in the <command>sendsignals</command> script for any
processes that are not shut down by their own scripts.  The delay for
<command>init</command> can be set by passing a parameter.  For example to
remove the delay in <command>init</command>, pass the -t0 parameter when
shutting down or rebooting (e.g.  <command>/sbin/shutdown -t0 -r
now</command>).  The delay for the <command>sendsignals</command> script can
be skipped by setting the parameter KILLDELAY=0.</para></listitem>

      </itemizedlist>

    </sect3>
  </sect2>
</sect1>
