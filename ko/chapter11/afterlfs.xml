<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../general.ent">
  %general-entities;
]>

<sect1 id="afterlfs">
  <?dbhtml filename="afterlfs.html"?>

  <title>LFS 이후 시작하기</title>

  <sect2>
  <title>다음에 할 것 결정하기</title>

     <para>
       이제 LFS가 완성되어 부팅 가능한 시스템을 갖추었으니, 무엇을 해야 할까요? 다음 단계는 이 시스템을 어떻게 사용할지 결정하는
것입니다. 일반적으로 워크스테이션과 서버 두 가지 큰 범주를 고려해 볼 수 있습니다. 사실 이 두 범주가 서로 배타적인 것은
아닙니다. 각 범주에 필요한 애플리케이션을 하나의 시스템에 통합할 수도 있지만, 지금은 각각 분리해서 살펴보겠습니다.
     </para>

     <para>
        서버는 더 단순한 범주에 속합니다. 일반적으로 이는 <ulink
url="&blfs-book;server/apache.html">Apache HTTP 서버</ulink>와 같은 웹 서버나 <ulink
url="&blfs-book;server/mariadb.html">MariaDB</ulink>와 같은 데이터베이스 서버로
구성됩니다. 하지만 다른 서비스도 가능합니다. 단일 목적 장치(single use device)에 내장된 운영 체제도 이 범주에
포함됩니다.
     </para>

     <para>
        반면, 워크스테이션은 훨씬 더 복잡합니다. 일반적으로 기초적인 <ulink
url="&blfs-book;x/installing.html">그래픽 환경</ulink>을 기반으로 <ulink
url="&blfs-book;lxde/lxde.html">LXDE</ulink>, <ulink
url="&blfs-book;xfce/xfce.html">XFCE</ulink>, <ulink
url="&blfs-book;kde/kde.html">KDE</ulink> 또는 <ulink
url="&blfs-book;gnome/gnome.html">Gnome</ulink>과 같은 그래픽 사용자 환경이 필요하며, <ulink
url="&blfs-book;xsoft/firefox.html">Firefox 웹 브라우저</ulink>, <ulink
url="&blfs-book;xsoft/thunderbird.html">Thunderbird 이메일 클라이언트</ulink> 또는
<ulink url="&blfs-book;xsoft/libreoffice.html">LibreOffice 오피스 제품군</ulink>과
같은 여러 그래픽 기반 애플리케이션도 필요합니다. 이러한 애플리케이션을 사용하려면 원하는 기능에 따라 수백 개의 추가 지원 애플리케이션과
라이브러리 패키지가 더 필요합니다.
     </para>

     <para>
        앞서 언급한 것들 외에도 모든 종류의 시스템을 관리하기 위한 일련의 애플리케이션이 있습니다. 이 애플리케이션은 모두 BLFS 책에
수록되어 있습니다. 모든 환경에서 모든 패키지가 필요한 것은 아닙니다. 예를 들어 <ulink
url="&blfs-book;basicnet/dhcpcd.html">dhcpcd</ulink>는 대개 서버에 적합하지 않으며,
<ulink url="&blfs-book;basicnet/wireless_tools.html">wireless_tools</ulink>는
일반적으로 노트북 시스템에서만 유용합니다. 
     </para>

  </sect2>

  <sect2>
  <title>기본 LFS 환경에서 작업</title>

    <para>
      처음 LFS로 부팅하면 추가 패키지를 빌드하는 데 필요한 내부 도구는 모두 갖춰져 있습니다. 아쉽게도 사용자 환경은 매우
빈약합니다. 이를 개선할 수 있는 몇 가지 방법:
    </para>

  <sect3>
  <title>chroot 환경에서 LFS 호스트를 이용한 작업</title>

    <para>
      이 방법은 기능이 풍부한 브라우저와 복사/붙여넣기 기능을 사용할 수 있는 완전한 그래픽 환경을 제공합니다. 또한 호스트 버전의 wget
같은 애플리케이션을 사용하여, chroot 환경에서 작업할 때 접근 가능한 위치로 패키지 소스를 다운로드할 수 있습니다.
    </para>

    <para>
       chroot 환경에서 패키지를 제대로 빌드하려면, 가상 파일 시스템이 아직 마운트 안 된 경우 이를 마운트해야 한다는 점을 기억해야
합니다. 이를 위한 한 가지 방법은 <emphasis role="bold">HOST</emphasis> 시스템에 스크립트를 생성하는
것입니다:
    </para>

<screen><command>cat &gt; ~/mount-virt.sh &lt;&lt; "EOF"
#!/bin/bash

function mountbind
{
   if ! mountpoint $LFS/$1 >/dev/null; then
     $SUDO mount --bind /$1 $LFS/$1
     echo $LFS/$1 mounted
   else
     echo $LFS/$1 already mounted
   fi
}

function mounttype
{
   if ! mountpoint $LFS/$1 >/dev/null; then
     $SUDO mount -t $2 $3 $4 $5 $LFS/$1
     echo $LFS/$1 mounted
   else
     echo $LFS/$1 already mounted
   fi
}

if [ $EUID -ne 0 ]; then
  SUDO=sudo
else
  SUDO=""
fi

if [ x$LFS == x ]; then
  echo "LFS not set"
  exit 1
fi

mountbind dev
mounttype dev/pts devpts devpts -o gid=5,mode=620
mounttype proc    proc   proc
mounttype sys     sysfs  sysfs
mounttype run     tmpfs  run
if [ -h $LFS/dev/shm ]; then
  install -v -d -m 1777 $LFS$(realpath /dev/shm)
else
  mounttype dev/shm tmpfs tmpfs -o nosuid,nodev
fi 

#mountbind usr/src
#mountbind boot
#mountbind home
EOF</command></screen>

    <para>
       스크립트에 마지막 세 명령어가 주석 처리되어 있음에 유의하십시오. 이 명령어는 해당 디렉터리가 호스트 시스템에서 별도 파티션으로
마운트되어 있으며, 완성된 LFS/BLFS 시스템을 부팅할 때 마운트될 경우에 쓸 만합니다.
    </para>

    <para>
        The script can be run with <command>bash ~/mount-virt.sh</command> as either
a regular user (recommended) or as &root;. If run as a regular user, sudo is
required on the host system.
    </para>

    <para>
        Another issue pointed out by the script is where to store downloaded package
files. This location is arbitrary. It can be in a regular user's home
directory such as ~/sources or in a global location like /usr/src. Our
recommendation is not to mix BLFS sources and LFS sources in (from the
chroot environment) /sources. In any case, the packages must be accessible
inside the chroot environment.
    </para>

    <para>
        A last convenience feature presented here is to streamline the process of
entering the chroot environment. This can be done with an alias placed in a
user's ~/.bashrc file on the host system:
    </para>

<screen><command>alias lfs='sudo /usr/sbin/chroot /mnt/lfs /usr/bin/env -i HOME=/root TERM="$TERM" PS1="\u:\w\\\\$ "
PATH=/usr/bin:/usr/sbin /bin/bash --login'</command></screen>

     <para>
       This alias is a little tricky because of the quoting and levels of backslash
characters. It must be all on a single line. The above command has been
split in two for presentation purposes.
    </para>

  </sect3>

  <sect3>
  <title>ssh 통한 원격 작업</title>

    <para>
       This method also provides a full graphical environment, but first requires
installing <ulink url="&blfs-book;postlfs/openssh.html">sshd</ulink> on the
LFS system, usually in chroot. It also requires a second computer. This
method has the advantage of being simple by not requiring the complexity of
the chroot environment. It also uses your LFS built kernel for all
additional packages and still provides a complete system for installing
packages.
    </para>

    <para>
      You may use the <command>scp</command> command to upload the package sources
to be built onto the LFS system.  If you want to download the sources onto
the LFS system directly instead, install <ulink
url="&blfs-book;general/libtasn1.html">libtasn1</ulink>, <ulink
url="&blfs-book;postlfs/p11-kit.html">p11-kit</ulink>, <ulink
url="&blfs-book;postlfs/make-ca.html">make-ca</ulink>, and <ulink
url="&blfs-book;basicnet/wget.html">wget</ulink> in chroot (or upload their
sources using <command>scp</command> after booting the LFS system).
    </para>
  </sect3>

  <sect3>
  <title>Work from the LFS command line</title>

    <para>
       This method requires installing <ulink
url="&blfs-book;general/libtasn1.html">libtasn1</ulink>, <ulink
url="&blfs-book;postlfs/p11-kit.html">p11-kit</ulink>, <ulink
url="&blfs-book;postlfs/make-ca.html">make-ca</ulink>, <ulink
url="&blfs-book;basicnet/wget.html">wget</ulink>, <ulink
url="&blfs-book;general/gpm.html">gpm</ulink>, and <ulink
url="&blfs-book;basicnet/links.html">links</ulink> (or <ulink
url="&blfs-book;basicnet/lynx.html">lynx</ulink>)  in chroot and then
rebooting into the new LFS system. At this point the default system has six
virtual consoles. Switching consoles is as easy as using the <keycombo>
<keycap>Alt</keycap> <keycap>Fx</keycap> </keycombo> key combinations where
<keycap>Fx</keycap> is between <keycap>F1</keycap> and <keycap>F6</keycap>.
The <keycombo> <keycap>Alt</keycap> <keycap function='left'/> </keycombo>
and <keycombo> <keycap>Alt</keycap> <keycap function='right'/> </keycombo>
combinations also will change the console.
     </para>

     <para>
       At this point you can log into two different virtual consoles and run the
links or lynx browser in one console and bash in the other. GPM then allows
copying commands from the browser with the left mouse button, switching
consoles, and pasting into the other console.
      </para>

      <note>
        <para>
          참고로, 가상 콘솔 간의 전환은 <keycombo> <keycap>Ctrl</keycap> <keycap>Alt</keycap>
<keycap>Fx</keycap> </keycombo> 키 조합을 사용하여 X 윈도우 인스턴스에서도 수행할 수 있지만, 그래픽
인터페이스와 가상 콘솔 사이에서는 마우스를 이용한 복사 안 됩니다. <keycombo> <keycap>Ctrl</keycap>
<keycap>Alt</keycap> <keycap>Fx</keycap> </keycombo> 조합을 사용하여 X 윈도우 화면으로 돌아갈
수 있으며, 여기서 <keycap>Fx</keycap>가 보통 <keycap>F1</keycap>이지만
<keycap>F7</keycap>일 수도 있습니다.
        </para>
      </note>

  </sect3>

  </sect2>


</sect1>
